apiVersion: apps/v1
kind: Deployment
metadata:
  name: filebrowser
spec:
  replicas: 1
  selector:
    matchLabels:
      app: filebrowser
  template:
    metadata:
      labels:
        app: filebrowser
    spec:
      containers:
        - name: filebrowser
          image: filebrowser/filebrowser:v2.32.0
          args:
            - "-c"
            - /var/config.json
          ports:
            - containerPort: 80
              name: http
          volumeMounts:
            - name: filebrowser-database
              mountPath: /data/database
            - name: filebrowser-config
              mountPath: /var

            - name: filebrowser-bag
              mountPath: /data/srv/bag

      volumes:
        - name: filebrowser-database
          persistentVolumeClaim:
            claimName: filebrowser-pvc-db
        - name: filebrowser-config
          configMap:
            name: filebrowser-config
            items:
              - key: config.json
                path: config.json

        - name: filebrowser-bag
          persistentVolumeClaim:
            claimName: filebrowser-pvc-bag

---
apiVersion: v1
kind: Service
metadata:
  name: filebrowser
spec:
  selector:
    app: filebrowser
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80